const fs=require('fs'),vm=require('vm'),assert=require('assert');const code=fs.readFileSync(__dirname+'/../frontend/js/app.js','utf8');const data=Array.from({length:1201},(_,n)=>({id:String(n).padStart(5,'0'),codigo:'F-'+n,apellido_paterno:n===0?'OBJETIVO':'OTRO',nombres:'SINTETICO',unidad:'PRUEBA',fecha_intervencion:'2026-01-01',creado_en:String(n).padStart(5,'0')}));
const result={innerHTML:'',textContent:'',querySelectorAll:()=>[]},controls={recordSearch:{value:'OBJETIVO'}};let calls=0;
const ctx={currentProfile:{id:'actor'},document:{getElementById:id=>id==='recordsResult'?result:controls[id]||{value:''}},supabaseClient:{from:()=>{let cursor='';return {select(){return this},order(){return this},limit(){return this},gt(k,v){cursor=v;return this},gte(){return this},lte(){return this},then(resolve){calls++;resolve({data:data.filter(r=>r.id>cursor).slice(0,500)})}}}},escapeHtml:String,formatDate:String,dashboardCategory:String,recordAge:()=>null};vm.createContext(ctx);vm.runInContext(code.slice(code.indexOf('let recordListTurn='),code.indexOf('const recordModal')),ctx);
(async()=>{await ctx.loadRecords();assert.equal(calls,4);assert(result.innerHTML.includes('OBJETIVO'));assert(result.innerHTML.includes('F-0'));console.log('PASS: oldest match beyond first 100/1000 rows remains searchable across 1201 authorized records.');})().catch(e=>{console.error(e);process.exitCode=1});
